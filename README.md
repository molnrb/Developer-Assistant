### Setup:

- Make sure you have a docker

- in Developer-Assistant-BE make a .env file based on .env.exmaple and add you OPENAI_API_KEY and SECRET_KEY (generate a random)

- to use preloaded projecs: user:test pass:test

### to start projet:

1. Start docker engine

2. run "docker compose up --build" form the project root